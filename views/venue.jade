<h1 id="page-title"><%= title %></h1>
<div id="list">
<form action="venue/create/" method="post" accept-charset="utf-8">
	<div class="item-new">
	<% for (var key in definitions.userData){ %>
		
		<% if( key == 'password' ){%>
		<input class="input" type="password" name="<%= key %>"  placeholder="<%= definitions.userData[key] %>"/><br>
		<% }else{ %>
		<input class="input" type="text" name="<%= key %>"  placeholder="<%= definitions.userData[key] %>"/><br>

		
	<%  }} %>

</div></div>
	<h2> Venue Info</h2>

<div id="list">
	<div class="item-new">
	<% for (var key in definitions.venueData){ %>
		
		<input class="input" type="text" name="<%= key %>"  placeholder="<%= definitions.venueData[key] %>"/><br>

		
	<%  } %>

</div>
<input type="submit" value="Submit">
</form>
</div>