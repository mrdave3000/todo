<h1 id="page-title"><%= title %></h1>
<div id="list">
<form action="artist/create/" method="post" accept-charset="utf-8">
	<div class="item-new">
	<% for (var key in definitions.userData){ %>
		
		<input class="input" type="text" name="<%= key %>"  placeholder="<%= definitions.userData[key] %>"/><br>

		
	<%  } %>

</div></div>
	<h2> Artist Info</h2>

<div id="list">
	<div class="item-new">
	<% for (var key in definitions.artistData){ %>
		
		<input class="input" type="text" name="<%= key %>"  placeholder="<%= definitions.artistData[key] %>"/><br>

		
	<%  } %>

</div>
<input type="submit" value="Submit">
</form>
</div>